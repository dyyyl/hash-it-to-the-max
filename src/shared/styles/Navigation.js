import styled from 'styled-components';

const Navigation = styled.span`
  font-size: 3rem;
  justify-self: center;
  align-self: center;

  &:hover {
    cursor: pointer;
  }
`;

export default Navigation;
